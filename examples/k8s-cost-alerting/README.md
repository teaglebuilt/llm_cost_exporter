# K8s cost alerting example

**Deploy Example**
> kustomize build --enable-helm --enable-exec . | kubectl apply -f -

**Destroy Example**
> kustomize build --enable-helm --enable-exec . | kubectl delete -f -